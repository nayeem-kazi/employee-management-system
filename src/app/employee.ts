export class Employee {
    constructor(public id: number, public name: string, public email: string, public gender: string, public dob: string, public designation: string, public department: string, public joiningDate: string, public salaryCode: number, public photo: string) {

    }
}
